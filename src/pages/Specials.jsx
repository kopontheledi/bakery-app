import {useEffect,useState} from 'react';import {supabase,isConfigured} from '../supabase';import SpecialCard from '../components/SpecialCard';
export default function Specials(){const [items,setItems]=useState([]);useEffect(()=>{(async()=>{if(!isConfigured)return;const {data}=await supabase.from('specials').select('*').order('created_at',{ascending:false});setItems((data||[]).filter(x=>!x.ends_at||new Date(`${x.ends_at}T23:59:59`)>=new Date()))})()},[]);return <div className="page section"><div className="page-head"><p className="eyebrow">Limited time</p><h1>Current specials</h1><p>Seasonal offers, celebration boxes and sweet deals while they last.</p></div>{items.length?<div className="special-grid">{items.map(x=><SpecialCard key={x.id} item={x}/>)}</div>:<div className="empty"><h3>No current specials</h3><p>Follow our social pages or check back soon for the next offer.</p></div>}</div>}
